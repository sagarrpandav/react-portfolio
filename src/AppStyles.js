import {makeStyles} from '@material-ui/core/styles';

export const useAppStyles = makeStyles(theme => ({
    root: {
        height: '100vh',
        backgroundImage: "url('https://react-resume-b1ac9.web.app/static/media/bg.278d9283.png')",
        backgroundColor: '#fcfcfc'
    }
}));